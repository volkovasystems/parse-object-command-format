try{ var base = window; }catch( error ){ var base = exports; }
( function module( base ){
	define( "parseObjectCommandFormat",
		[
		],
		function construct( ){
			var parseObjectCommandFormat = function parseObjectCommandFormat( data ){
				
			};

			base.parseObjectCommandFormat = parseObjectCommandFormat;
		} );
} )( base );
